apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# k8s-dashboard creates its own namespace by default; pin it to
# kube-system to keep platform infra co-located.
namespace: kube-system

resources:
  - ../base
  - ingress.yml

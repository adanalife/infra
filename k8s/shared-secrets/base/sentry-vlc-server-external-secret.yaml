# Sentry DSN for the vlc-server Go service. Sourced from AWS Secrets
# Manager (k8s/sentry-vlc-server) via the cluster-wide aws-secretsmanager
# ClusterSecretStore. Materialized Secret carries SENTRY_DSN; the
# vlc-server Deployment envFroms it with optional: false.
#
# Per vault/decisions/observability-projects-by-component.md, the same
# DSN value lives in stage and prod SM containers (one Sentry project
# per component; env partitioning via SENTRY_ENVIRONMENT on each event).
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: sentry-vlc-server
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secretsmanager
    kind: SecretStore
  target:
    name: sentry-vlc-server
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: k8s/sentry-vlc-server

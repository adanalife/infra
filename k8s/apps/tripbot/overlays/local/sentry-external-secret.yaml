# ExternalSecret for the tripbot Sentry DSN. Sourced from AWS Secrets
# Manager (k8s/sentry-tripbot) via the cluster-wide aws-secretsmanager
# ClusterSecretStore. The remote SM value is JSON; dataFrom.extract pulls
# the SENTRY_DSN key into the materialized Secret so the Deployment can
# `envFrom: secretRef:` without spelling each var here.
#
# Resource name is `sentry-tripbot` (not just `sentry`) so it doesn't
# collide with vlc-server's ExternalSecret when the stage-1 overlay
# aggregates both apps into the same namespace.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: sentry-tripbot
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore
  target:
    name: sentry-tripbot
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: k8s/sentry-tripbot

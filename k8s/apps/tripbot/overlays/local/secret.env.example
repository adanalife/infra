# Copy this file to `secret.env` (gitignored) and fill in the local
# Postgres DB creds. THIS IS THE ONLY SECRET FILE ON DISK — everything
# else (Twitch, Maps, Sentry, OTLP) comes from ESO/AWS Secrets Manager
# via the base overlay, the same as the cloud environments. These three
# must match the local postgres overlay's secret.env.
DATABASE_USER=tripbot_docker
DATABASE_PASS=hunter2
DATABASE_DB=tripbot_docker

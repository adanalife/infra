# Public Ingress for vlc-server's HTTP API at vlc.dev.whereisdana.today.
#
# external-dns reconciles the hostname annotation into a Route53 A
# record pointing at the host's public IPv4 (set globally via
# external-dns's --default-targets, fed from values.local.yml).
#
# HTTP-only today. cert-manager is installed and proven (the smoke-test
# Certificate in k8s/cert-manager/development/ issues against LE staging on
# every cluster bring-up). The remaining blocker is the home-network
# port-forward — once inbound 443 reaches traefik, uncomment the
# cert-manager.io/cluster-issuer annotation AND the tls: block below to
# flip this URL to HTTPS. Start with letsencrypt-staging-route53 to
# avoid LE prod's per-week duplicate-cert limit; flip to
# letsencrypt-route53 once the first cert issues clean.
#
# BEFORE FLIPPING TO HTTPS: vlc-server has zero in-process auth on its
# write-side endpoints (skip/back/play, onscreens overlays). Anyone who
# can reach this URL can disrupt the live stream. Audit + remediation
# options (keep-internal vs. shared-secret vs. IP-allowlist) tracked
# in vault/tripbot/http-endpoints.md and vault/tripbot/vlc-server/TODO.md.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vlc-server
  annotations:
    external-dns.alpha.kubernetes.io/hostname: vlc.dev.whereisdana.today
    # cert-manager.io/cluster-issuer: letsencrypt-staging-route53
spec:
  ingressClassName: traefik
  # tls:
  #   - hosts:
  #       - vlc.dev.whereisdana.today
  #     secretName: vlc-tls
  rules:
    - host: vlc.dev.whereisdana.today
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vlc-server
                port:
                  name: http

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - host-access.yaml

# secret.env is gitignored — copy from secret.env.example.
secretGenerator:
  - name: obs-secret
    envs:
      - secret.env

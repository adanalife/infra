name: terraform-drift

on:
  schedule:
    # every Monday morning
    - cron:  "0 8 * * 1"
  pull_request:
    paths:
      - '.github/workflows/terraform-drift.yml'

permissions:
  contents: read
  # GCP Workload Identity Federation (stage-1 / prod-1 google provider auth)
  id-token: write

jobs:
  check_drift:
    # Dependabot PRs run with an isolated, empty secret namespace; skip the
    # AWS-touching drift check. The scheduled cron run is unaffected.
    if: github.actor != 'dependabot[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      # Don't let one workspace's drift cancel the others — mirror terraform.yml
      # so a prod-1 failure doesn't take stage-1's check down with it.
      fail-fast: false
      matrix:
        terraform-dirs: [terraform/core, terraform/prod-1, terraform/stage-1]
    name: Check for drift of terraform configuration
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - run: ./.github/scripts/set-deploy-env.sh ${{ matrix.terraform-dirs }}
        env:
          CI_CORE_AWS_ACCESS_KEY_ID: ${{ secrets.CI_CORE_AWS_ACCESS_KEY_ID }}
          CI_CORE_AWS_SECRET_ACCESS_KEY: ${{ secrets.CI_CORE_AWS_SECRET_ACCESS_KEY }}
          CI_PROD_AWS_ACCESS_KEY_ID: ${{ secrets.CI_PROD_AWS_ACCESS_KEY_ID }}
          CI_PROD_AWS_SECRET_ACCESS_KEY: ${{ secrets.CI_PROD_AWS_SECRET_ACCESS_KEY }}
          CI_STAGE_AWS_ACCESS_KEY_ID: ${{ secrets.CI_STAGE_AWS_ACCESS_KEY_ID }}
          CI_STAGE_AWS_SECRET_ACCESS_KEY: ${{ secrets.CI_STAGE_AWS_SECRET_ACCESS_KEY }}
      - uses: aws-actions/configure-aws-credentials@v6
        with:
          aws-region: us-east-1
          aws-access-key-id: ${{ env.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ env.AWS_SECRET_ACCESS_KEY }}
          role-to-assume: CITerraformRole
      # stage-1 / prod-1 carry a google provider that authenticates keyless via
      # Workload Identity Federation (GitHub OIDC -> the per-project terraform
      # SA), so the drift check needs the same GCP auth the plan workflow does —
      # without it `terraform plan` fails resolving GCP ADC. core has no google
      # provider, so it skips this. Mirrors terraform.yml's plan job.
      - name: GCP WIF target
        if: matrix.terraform-dirs == 'terraform/stage-1' || matrix.terraform-dirs == 'terraform/prod-1'
        run: |
          case "${{ matrix.terraform-dirs }}" in
            terraform/prod-1)
              echo "WIF_PROVIDER=projects/876608406330/locations/global/workloadIdentityPools/github/providers/github" >> "$GITHUB_ENV"
              echo "WIF_SA=terraform@tripbot-prod.iam.gserviceaccount.com" >> "$GITHUB_ENV" ;;
            terraform/stage-1)
              echo "WIF_PROVIDER=projects/574760983858/locations/global/workloadIdentityPools/github/providers/github" >> "$GITHUB_ENV"
              echo "WIF_SA=terraform@tripbot-stage.iam.gserviceaccount.com" >> "$GITHUB_ENV" ;;
          esac
          echo "TF_VAR_gcp_impersonate=false" >> "$GITHUB_ENV"
      - name: GCP auth (WIF)
        id: gcp_auth
        if: matrix.terraform-dirs == 'terraform/stage-1' || matrix.terraform-dirs == 'terraform/prod-1'
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ env.WIF_PROVIDER }}
          service_account: ${{ env.WIF_SA }}
          token_format: access_token
      - name: Export GCP token for terraform
        if: steps.gcp_auth.outcome == 'success'
        run: echo "GOOGLE_OAUTH_ACCESS_TOKEN=${{ steps.gcp_auth.outputs.access_token }}" >> "$GITHUB_ENV"
      - name: Check for drift
        uses: dflook/terraform-check@v3
        with:
          path: ${{ matrix.terraform-dirs }}

  # A scheduled run has no PR to turn red, so a failure's only record is an
  # entry in a run list nobody is watching — stage-1 drifted unreported for five
  # weeks that way. Restricted to `schedule` on purpose: the PR runs of this
  # workflow are already visible as a check.
  #
  # `if: failure()` catches whichever matrix leg drifted; the message points at
  # the log rather than naming a workspace, since any of them can be the one.
  # Best-effort like the app repos' release alert — continue-on-error keeps a
  # rotated webhook from turning a failure to report into a second failure.
  alert:
    needs: [check_drift]
    if: failure() && github.event_name == 'schedule'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Alert Discord on drift
        continue-on-error: true
        uses: Ilshidur/action-discord@0.4.0
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK_ALERTS }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        with:
          args: 'Terraform drift in [{{ EVENT_PAYLOAD.repository.full_name }}]({{ EVENT_PAYLOAD.repository.html_url }}): [run log]({{ RUN_URL }})'
